# Local runtime model configuration
# SINGLE SOURCE OF TRUTH for role → model mapping
#
# INVARIANT: All agents depend on roles, not models.
# Changing any model here requires ZERO code changes.

# ============================================================================
# Classification & Routing (fast, cheap)
# ============================================================================

intent:
  provider: ollama
  model: mistral:7b-instruct
  base_url: http://localhost:11434

classifier:
  provider: ollama
  model: mistral:7b-instruct
  base_url: http://localhost:11434

# ============================================================================
# Reasoning & Planning (strong reasoning)
# ============================================================================

goal_interpreter:
  provider: ollama
  model: mistral:7b-instruct
  base_url: http://localhost:11434

coordinator:
  provider: ollama
  model: mistral:7b-instruct
  base_url: http://localhost:11434

planner:
  provider: ollama
  model: mistral:7b-instruct
  base_url: http://localhost:11434

tool_resolver:
  provider: ollama
  model: mistral:7b-instruct
  base_url: http://localhost:11434

# ============================================================================
# Response & Presentation
# ============================================================================

response:
  provider: ollama
  model: mistral:7b-instruct
  base_url: http://localhost:11434

# ============================================================================
# Validation & Advanced Decomposition
# ============================================================================

critic:
  provider: ollama
  model: llama3:8b
  base_url: http://localhost:11434

tda:
  provider: ollama
  model: mistral:7b-instruct
  base_url: http://localhost:11434

# ============================================================================
# Phase 5: Plan Repair (conservative, low temperature)
# ============================================================================

repair:
  provider: ollama
  model: llama3:8b
  base_url: http://localhost:11434
 # Conservative for repair reasoning
